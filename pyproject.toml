[project]
name = "lab01"
version = "0.1.0"
description = "Neural networks lab 01"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn",
    "torch",
    "jupyter",
    "ipykernel",
]

[dependency-groups]
dev = []
